#include "Music.h";
#include "Songbook.h";

task main {
	PlayTune(czardas1);
	PlayTune(czardas1);
	PlayTune(czardas2);
	PlayTune(czardas3);
	PlayTune(czardas2);
	PlayTune(czardas3);
	PlayTune(czardas4);
	PlayTune(czardas5);
}
